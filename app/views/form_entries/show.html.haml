.forms
  %h1 
    Form Entry #
    = @form_entry.id

  %h2
    Client: 
    = @form_entry.cross_reference(xref_type: :client).display_name

  %h1
    Responses

  - @disp_array[:answers].each do |disp|
    %hr
    %span.form_question
      = disp[:dv]
    %span.form_answer
      = disp[:ans]

  
